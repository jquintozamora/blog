- var top_img = theme.top_img || config.top_img
- var flag = top_img === true ? 'no-bg' : top_img
nav#nav(style=`background-image: url(${top_img})`) 
  include ./header.pug
  #site-info
    #site-title= "Hello, I'm José" 
    #site-sub-title= config.subtitle
